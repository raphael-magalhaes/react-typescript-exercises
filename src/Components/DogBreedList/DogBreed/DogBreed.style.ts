import { createUseStyles } from 'react-jss'

export const useClasses = createUseStyles({
    container: {
        width: '19rem',
        backgroundColor: '#fafafa'
    },
    name: {
        margin: 0,
        color: '#424242'
    }
})
