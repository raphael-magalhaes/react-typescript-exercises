import { createUseStyles } from 'react-jss'

export const useClasses = createUseStyles({
    container: {
        width: '19rem',
        height: '25rem',
        overflowY: 'scroll',
        backgroundColor: '#fafafa'
    }
})
